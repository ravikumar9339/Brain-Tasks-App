apiVersion: apps/v1
kind: Deployment
metadata:
  name: brain-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: brain
  template:
    metadata:
      labels:
        app: brain
    spec:
      containers:
      - name: brain
        image: 251221984734.dkr.ecr.ap-south-1.amazonaws.com/brain-tasks-app
        ports:
        - containerPort: 80
